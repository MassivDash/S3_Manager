
pre-commit:
  parallel: false
  commands:
    eslint:
      glob:  "*.{.svelte, ts, js}"
      run: ./node_modules/.bin/eslint --fix 
    prettier:
      glob:  "*.{.svelte, ts, js}"
      run: ./node_modules/.bin/prettier --write {staged_files}
